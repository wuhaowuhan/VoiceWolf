# 游戏版型选择功能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在游戏开始前添加版型选择流程，版型决定可标记的角色类型，人数范围调整为12-15人。

**Architecture:** 新增SetupActivity作为启动页，选择版型后跳转到MainActivity。版型数据通过Intent传递，存储在GameViewModel中，影响角色弹窗和模板按钮的显示。

**Tech Stack:** Kotlin, Android SDK 34, MVVM, ViewBinding, Material Design

---

## File Structure

**新增文件：**
- `app/src/main/java/com/voicewolf/app/GameSetup.kt` - 版型数据类和预置常量
- `app/src/main/java/com/voicewolf/app/SetupActivity.kt` - 版型选择页面
- `app/src/main/res/layout/activity_setup.xml` - 版型选择页面布局
- `app/src/main/res/layout/item_setup_card.xml` - 版型卡片布局（可复用）

**修改文件：**
- `app/src/main/java/com/voicewolf/app/Player.kt` - MarkedRole枚举新增3个角色
- `app/src/main/java/com/voicewolf/app/GameViewModel.kt` - 新增currentSetup，人数改为12-15
- `app/src/main/java/com/voicewolf/app/MainActivity.kt` - 接收版型、重置三选项、动态角色弹窗
- `app/src/main/AndroidManifest.xml` - SetupActivity设为LAUNCHER
- `app/src/main/res/values/colors.xml` - 新增3个角色颜色

---

### Task 1: 新增版型数据类 GameSetup.kt

**Files:**
- Create: `app/src/main/java/com/voicewolf/app/GameSetup.kt`

- [ ] **Step 1: 创建GameSetup.kt文件**

```kotlin
package com.voicewolf.app

/**
 * Represents a game setup/configuration with predefined roles
 */
data class GameSetup(
    val name: String,              // 版型名称
    val goodRoles: List<SetupRole>, // 好人阵营角色
    val evilRoles: List<SetupRole>  // 狼人阵营角色
) {
    // 获取所有角色（用于角色选择弹窗）
    fun getAllRoles(): List<SetupRole> = goodRoles + evilRoles
    
    // 获取版型显示摘要（好人阵营角色列表）
    fun getGoodRolesSummary(): String = goodRoles.map { "${it.role.getDisplayName()}${if (it.count > 1) it.count else ""}" }.joinToString("/")
    
    // 获取版型显示摘要（狼人阵营角色列表）
    fun getEvilRolesSummary(): String = evilRoles.map { "${it.role.getDisplayName()}${if (it.count > 1) it.count else ""}" }.joinToString("/")
    
    // 判断版型是否包含某个角色
    fun hasRole(role: Player.MarkedRole): Boolean = getAllRoles().any { it.role == role }
}

/**
 * Represents a role in the setup with count
 */
data class SetupRole(
    val role: Player.MarkedRole,
    val count: Int
)

/**
 * Preset game setups
 */
object GameSetupPresets {
    // 狼王守卫版型
    val WOLF_KING_GUARD = GameSetup(
        name = "狼王守卫",
        goodRoles = listOf(
            SetupRole(Player.MarkedRole.SEER, 1),
            SetupRole(Player.MarkedRole.WITCH, 1),
            SetupRole(Player.MarkedRole.GUARD, 1),
            SetupRole(Player.MarkedRole.HUNTER, 1),
            SetupRole(Player.MarkedRole.VILLAGER, 4)
        ),
        evilRoles = listOf(
            SetupRole(Player.MarkedRole.WEREWOLF, 3),
            SetupRole(Player.MarkedRole.WOLF_KING, 1)
        )
    )
    
    // 预女猎白版型
    val SEER_WITCH_HUNTER_IDIOT = GameSetup(
        name = "预女猎白",
        goodRoles = listOf(
            SetupRole(Player.MarkedRole.SEER, 1),
            SetupRole(Player.MarkedRole.WITCH, 1),
            SetupRole(Player.MarkedRole.HUNTER, 1),
            SetupRole(Player.MarkedRole.IDIOT, 1),
            SetupRole(Player.MarkedRole.VILLAGER, 4)
        ),
        evilRoles = listOf(
            SetupRole(Player.MarkedRole.WEREWOLF, 4)
        )
    )
    
    // 狼美骑士版型
    val WOLF_BEAUTY_KNIGHT = GameSetup(
        name = "狼美骑士",
        goodRoles = listOf(
            SetupRole(Player.MarkedRole.SEER, 1),
            SetupRole(Player.MarkedRole.WITCH, 1),
            SetupRole(Player.MarkedRole.GUARD, 1),
            SetupRole(Player.MarkedRole.KNIGHT, 1),
            SetupRole(Player.MarkedRole.VILLAGER, 4)
        ),
        evilRoles = listOf(
            SetupRole(Player.MarkedRole.WEREWOLF, 3),
            SetupRole(Player.MarkedRole.WOLF_BEAUTY, 1)
        )
    )
    
    // 通灵师机械狼版型
    val SEER_MIRROR_MECH_WOLF = GameSetup(
        name = "通灵师机械狼",
        goodRoles = listOf(
            SetupRole(Player.MarkedRole.SEER_MIRROR, 1),
            SetupRole(Player.MarkedRole.WITCH, 1),
            SetupRole(Player.MarkedRole.HUNTER, 1),
            SetupRole(Player.MarkedRole.GUARD, 1),
            SetupRole(Player.MarkedRole.VILLAGER, 4)
        ),
        evilRoles = listOf(
            SetupRole(Player.MarkedRole.WEREWOLF, 3),
            SetupRole(Player.MarkedRole.MECHANICAL_WOLF, 1)
        )
    )
    
    // 所有预置版型列表
    val ALL = listOf(
        WOLF_KING_GUARD,
        SEER_WITCH_HUNTER_IDIOT,
        WOLF_BEAUTY_KNIGHT,
        SEER_MIRROR_MECH_WOLF
    )
    
    // 根据名称获取版型
    fun getByName(name: String): GameSetup? = ALL.find { it.name == name }
}
```

- [ ] **Step 2: 验证文件编译通过**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL（此时会因Player.MarkedRole缺少新角色而失败，下一步修复）

---

### Task 2: 扩展Player.MarkedRole枚举

**Files:**
- Modify: `app/src/main/java/com/voicewolf/app/Player.kt`

- [ ] **Step 1: 修改MarkedRole枚举，新增SEER_MIRROR、IDIOT、WOLF_KING**

在`Player.kt`的`MarkedRole`枚举中，将现有枚举替换为：

```kotlin
enum class MarkedRole {
    NONE,
    // 好人阵营
    SEER,           // 预言家
    SEER_MIRROR,    // 通灵师（新增）
    WITCH,          // 女巫
    HUNTER,         // 猎人
    GUARD,          // 守卫
    KNIGHT,         // 骑士
    IDIOT,          // 白痴（新增）
    VILLAGER,       // 平民
    GOOD,           // 好人（泛指）
    // 狼人阵营
    WEREWOLF,       // 狼人
    WOLF_KING,      // 狼王（新增）
    WOLF_BEAUTY,    // 狼美人
    MECHANICAL_WOLF // 机械狼
}
```

- [ ] **Step 2: 修改getMarkedRoleDisplayName()方法，添加新角色显示名**

在`getMarkedRoleDisplayName()`方法中，添加新角色的显示名：

```kotlin
fun getMarkedRoleDisplayName(): String {
    return when (markedRole) {
        MarkedRole.NONE -> ""
        MarkedRole.SEER -> "预言"
        MarkedRole.SEER_MIRROR -> "通灵"
        MarkedRole.GOOD -> "好人"
        MarkedRole.WEREWOLF -> "狼人"
        MarkedRole.VILLAGER -> "平民"
        MarkedRole.WITCH -> "女巫"
        MarkedRole.HUNTER -> "猎人"
        MarkedRole.GUARD -> "守卫"
        MarkedRole.KNIGHT -> "骑士"
        MarkedRole.IDIOT -> "白痴"
        MarkedRole.WOLF_KING -> "狼王"
        MarkedRole.WOLF_BEAUTY -> "狼美"
        MarkedRole.MECHANICAL_WOLF -> "机械狼"
    }
}
```

- [ ] **Step 3: 修改isMarkedEvil()方法，狼王属于狼人阵营**

```kotlin
fun isMarkedEvil(): Boolean {
    return markedRole in listOf(MarkedRole.WEREWOLF, MarkedRole.WOLF_KING, MarkedRole.WOLF_BEAUTY, MarkedRole.MECHANICAL_WOLF)
}
```

- [ ] **Step 4: 验证编译通过**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 5: Commit**

```bash
git add app/src/main/java/com/voicewolf/app/Player.kt app/src/main/java/com/voicewolf/app/GameSetup.kt
git commit -m "$(cat <<'EOF'
feat: 新增版型数据类和MarkedRole枚举扩展

- 新增GameSetup.kt定义版型数据结构和4种预置版型
- MarkedRole新增SEER_MIRROR(通灵师)、IDIOT(白痴)、WOLF_KING(狼王)
- 更新角色显示名和阵营判断方法

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 新增角色颜色定义

**Files:**
- Modify: `app/src/main/res/values/colors.xml`

- [ ] **Step 1: 在colors.xml末尾添加新角色颜色**

```xml
    <!-- 新增角色颜色 -->
    <color name="role_seer_mirror">#FF06B6D4</color>  <!-- 通灵师 - 青色 -->
    <color name="role_idiot">#FFA3A3A3</color>        <!-- 白痴 - 浅灰 -->
    <color name="role_knight">#FFD4AF37</color>       <!-- 骑士 - 金色 -->
    <color name="role_wolf_king">#FF7F1D1D</color>    <!-- 狼王 - 深红 -->
    <color name="role_wolf_beauty">#FFBE185D</color>  <!-- 狼美人 - 粉红 -->
</resources>
```

- [ ] **Step 2: Commit**

```bash
git add app/src/main/res/values/colors.xml
git commit -m "$(cat <<'EOF'
feat: 新增通灵师、白痴、骑士、狼王、狼美人角色颜色

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: 修改GameViewModel人数范围和版型状态

**Files:**
- Modify: `app/src/main/java/com/voicewolf/app/GameViewModel.kt`

- [ ] **Step 1: 修改MIN_PLAYERS常量从8改为12**

在`GameViewModel.kt`的`companion object`中：

```kotlin
companion object {
    const val MIN_PLAYERS = 12  // 改为12
    const val DEFAULT_PLAYERS = 12
    const val MAX_PLAYERS = 15
}
```

- [ ] **Step 2: 新增currentSetup LiveData**

在类的属性声明区域，添加：

```kotlin
// Current game setup
private val _currentSetup = MutableLiveData<GameSetup>()
val currentSetup: LiveData<GameSetup> = _currentSetup
```

- [ ] **Step 3: 新增setSetup方法**

在类中添加方法：

```kotlin
// Set game setup
fun setSetup(setup: GameSetup) {
    _currentSetup.value = setup
}
```

- [ ] **Step 4: 修改resetGame方法，保持当前版型**

将`resetGame()`方法改为：

```kotlin
fun resetGame(keepSetup: Boolean = true) {
    _currentDay.value = 1
    _speechRecords.value = mutableListOf()
    _voteRecords.value = mutableListOf()
    _allRecordsText.value = ""
    
    // Reset to default 12 players
    val resetPlayers = (1..DEFAULT_PLAYERS).map { Player(id = it) }
    _players.value = resetPlayers
    updateActivePlayerIds()
    updateDisplayInfo()
    
    // Clear setup if not keeping
    if (!keepSetup) {
        _currentSetup.value = null
    }
}
```

- [ ] **Step 5: 验证编译通过**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 6: Commit**

```bash
git add app/src/main/java/com/voicewolf/app/GameViewModel.kt
git commit -m "$(cat <<'EOF'
feat: GameViewModel新增版型状态和人数范围调整

- MIN_PLAYERS从8改为12
- 新增currentSetup LiveData存储当前版型
- 新增setSetup方法
- resetGame支持是否保持版型参数

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 创建SetupActivity布局

**Files:**
- Create: `app/src/main/res/layout/activity_setup.xml`

- [ ] **Step 1: 创建activity_setup.xml布局文件**

```xml
<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_dark"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <!-- App Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="VoiceWolf"
            android:textColor="@color/white"
            android:textSize="32sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="狼人杀面杀助手"
            android:textColor="@color/gray_light"
            android:textSize="16sp"
            android:layout_marginBottom="32dp" />

        <!-- Section Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="选择游戏版型"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp" />

        <!-- Setup Cards Container -->
        <LinearLayout
            android:id="@+id/setupCardsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

    </LinearLayout>
</ScrollView>
```

- [ ] **Step 2: Commit**

```bash
git add app/src/main/res/layout/activity_setup.xml
git commit -m "$(cat <<'EOF'
feat: 新增SetupActivity布局文件

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 创建SetupActivity

**Files:**
- Create: `app/src/main/java/com/voicewolf/app/SetupActivity.kt`

- [ ] **Step 1: 创建SetupActivity.kt**

```kotlin
package com.voicewolf.app

import android.content.Intent
import android.os.Bundle
import android.view.Gravity
import android.widget.LinearLayout
import android.widget.TextView
import androidx.appcompat.app.AppCompatActivity
import androidx.cardview.widget.CardView
import com.google.android.material.card.MaterialCardView

/**
 * Activity for selecting game setup before starting the game
 */
class SetupActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_setup)
        
        setupSetupCards()
    }
    
    private fun setupSetupCards() {
        val container = findViewById<LinearLayout>(R.id.setupCardsContainer)
        
        GameSetupPresets.ALL.forEach { setup ->
            val card = createSetupCard(setup)
            container.addView(card)
        }
    }
    
    private fun createSetupCard(setup: GameSetup): MaterialCardView {
        val card = MaterialCardView(this).apply {
            layoutParams = LinearLayout.LayoutParams(
                LinearLayout.LayoutParams.MATCH_PARENT,
                LinearLayout.LayoutParams.WRAP_CONTENT
            ).apply {
                setMargins(0, 0, 0, 16)
            }
            radius = 12f
            cardElevation = 4f
            setCardBackgroundColor(resources.getColor(R.color.info_panel, null))
            strokeWidth = 2
            strokeColor = resources.getColor(R.color.border_default, null)
            isClickable = true
            isFocusable = true
            
            setOnClickListener {
                startMainActivity(setup)
            }
        }
        
        val contentLayout = LinearLayout(this).apply {
            orientation = LinearLayout.VERTICAL
            padding = 16
            layoutParams = LinearLayout.LayoutParams(
                LinearLayout.LayoutParams.MATCH_PARENT,
                LinearLayout.LayoutParams.WRAP_CONTENT
            )
        }
        
        // Setup name
        val nameText = TextView(this).apply {
            text = setup.name
            setTextColor(resources.getColor(R.color.white, null))
            textSize = 18f
            gravity = Gravity.CENTER
            layoutParams = LinearLayout.LayoutParams(
                LinearLayout.LayoutParams.MATCH_PARENT,
                LinearLayout.LayoutParams.WRAP_CONTENT
            ).apply {
                setMargins(0, 0, 0, 8)
            }
        }
        contentLayout.addView(nameText)
        
        // Good roles summary
        val goodText = TextView(this).apply {
            text = "好人：${setup.getGoodRolesSummary()}"
            setTextColor(resources.getColor(R.color.border_good, null))
            textSize = 14f
            gravity = Gravity.CENTER
        }
        contentLayout.addView(goodText)
        
        // Evil roles summary
        val evilText = TextView(this).apply {
            text = "狼人：${setup.getEvilRolesSummary()}"
            setTextColor(resources.getColor(R.color.wolf_red, null))
            textSize = 14f
            gravity = Gravity.CENTER
        }
        contentLayout.addView(evilText)
        
        card.addView(contentLayout)
        return card
    }
    
    private fun startMainActivity(setup: GameSetup) {
        val intent = Intent(this, MainActivity::class.java).apply {
            putExtra("SETUP_NAME", setup.name)
        }
        startActivity(intent)
        finish()
    }
}
```

- [ ] **Step 2: 验证编译通过**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: Commit**

```bash
git add app/src/main/java/com/voicewolf/app/SetupActivity.kt
git commit -m "$(cat <<'EOF'
feat: 新增SetupActivity版型选择页面

- 显示4种预置版型卡片
- 点击卡片跳转MainActivity并传递版型名称

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 修改AndroidManifest设置启动Activity

**Files:**
- Modify: `app/src/main/AndroidManifest.xml`

- [ ] **Step 1: 添加SetupActivity声明并设为LAUNCHER**

将`AndroidManifest.xml`修改为：

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceWolf">
        
        <!-- SetupActivity as launcher -->
        <activity
            android:name=".SetupActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- MainActivity (no launcher intent) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
    </application>

</manifest>
```

- [ ] **Step 2: 验证编译通过**

Run: `./gradlew assembleDebug`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: Commit**

```bash
git add app/src/main/AndroidManifest.xml
git commit -m "$(cat <<'EOF'
feat: SetupActivity设为APP启动入口

- SetupActivity添加LAUNCHER intent-filter
- MainActivity移除LAUNCHER，改为exported=false

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: MainActivity接收版型Intent并初始化

**Files:**
- Modify: `app/src/main/java/com/voicewolf/app/MainActivity.kt`

- [ ] **Step 1: 在MainActivity.onCreate中接收版型并设置ViewModel**

在`onCreate`方法中，在`viewModel = ViewModelProvider(this)[GameViewModel::class.java]`之后添加：

```kotlin
// Receive setup from intent
val setupName = intent.getStringExtra("SETUP_NAME")
if (setupName != null) {
    val setup = GameSetupPresets.getByName(setupName)
    if (setup != null) {
        viewModel.setSetup(setup)
    }
}
```

- [ ] **Step 2: 添加setupName属性保存版型名称**

在类的开头添加属性：

```kotlin
private var setupName: String? = null
```

并在onCreate中赋值：

```kotlin
setupName = intent.getStringExtra("SETUP_NAME")
```

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/voicewolf/app/MainActivity.kt
git commit -m "$(cat <<'EOF'
feat: MainActivity接收版型Intent并初始化ViewModel

- onCreate中获取SETUP_NAME并设置ViewModel

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: 修改角色选择弹窗动态显示版型角色

**Files:**
- Modify: `app/src/main/java/com/voicewolf/app/MainActivity.kt:182-230`

- [ ] **Step 1: 重写showMarkRoleDialog方法**

将`showMarkRoleDialog`方法完整替换为：

```kotlin
private fun showMarkRoleDialog(playerId: Int) {
    val setup = viewModel.currentSetup.value
    if (setup == null) {
        Toast.makeText(this, "请先选择游戏版型", Toast.LENGTH_SHORT).show()
        return
    }
    
    val currentPlayer = viewModel.getPlayerById(playerId)
    val currentMarkedRole = currentPlayer?.markedRole ?: Player.MarkedRole.NONE
    
    // Build role options from setup
    val goodRoles = setup.goodRoles.map { it.role }
    val evilRoles = setup.evilRoles.map { it.role }
    
    // Create custom dialog view
    val dialogView = android.view.View.inflate(this, R.layout.dialog_mark_role, null)
    val goodContainer = dialogView.findViewById<LinearLayout>(R.id.goodRolesContainer)
    val evilContainer = dialogView.findViewById<LinearLayout>(R.id.evilRolesContainer)
    
    // Add good role buttons
    goodRoles.forEach { role ->
        addRoleButton(goodContainer, role, playerId, currentMarkedRole)
    }
    
    // Add evil role buttons
    evilRoles.forEach { role ->
        addRoleButton(evilContainer, role, playerId, currentMarkedRole)
    }
    
    val dialog = AlertDialog.Builder(this)
        .setTitle("标记身份 - ${playerId}号")
        .setView(dialogView)
        .setNegativeButton("取消", null)
        .create()
    
    // Store dialog reference for button clicks to close
    roleDialog = dialog
    dialog.show()
}

// Helper to add role button
private var roleDialog: AlertDialog? = null

private fun addRoleButton(container: LinearLayout, role: Player.MarkedRole, playerId: Int, currentRole: Player.MarkedRole) {
    val button = com.google.android.material.button.MaterialButton(this).apply {
        text = role.getDisplayName()
        layoutParams = LinearLayout.LayoutParams(
            LinearLayout.LayoutParams.WRAP_CONTENT,
            LinearLayout.LayoutParams.WRAP_CONTENT
        ).apply {
            setMargins(4, 4, 4, 4)
        }
        
        // Highlight if currently selected
        if (role == currentRole) {
            strokeWidth = 2
            strokeColor = resources.getColor(R.color.white, null)
        }
        
        // Set color based on faction
        val bgColor = getRoleBackgroundColor(role)
        setBackgroundColor(bgColor)
        setTextColor(resources.getColor(R.color.white, null))
        
        setOnClickListener {
            viewModel.setPlayerMarkedRole(playerId, role)
            updatePlayerView(playerId)
            roleDialog?.dismiss()
        }
    }
    container.addView(button)
}

private fun getRoleBackgroundColor(role: Player.MarkedRole): Int {
    val colorRes = when (role) {
        Player.MarkedRole.SEER -> R.color.role_seer
        Player.MarkedRole.SEER_MIRROR -> R.color.role_seer_mirror
        Player.MarkedRole.WITCH -> R.color.role_witch
        Player.MarkedRole.HUNTER -> R.color.role_hunter
        Player.MarkedRole.GUARD -> R.color.role_guard
        Player.MarkedRole.KNIGHT -> R.color.role_knight
        Player.MarkedRole.IDIOT -> R.color.role_idiot
        Player.MarkedRole.VILLAGER -> R.color.role_villager
        Player.MarkedRole.GOOD -> R.color.role_good
        Player.MarkedRole.WEREWOLF -> R.color.role_werewolf
        Player.MarkedRole.WOLF_KING -> R.color.role_wolf_king
        Player.MarkedRole.WOLF_BEAUTY -> R.color.role_wolf_beauty
        Player.MarkedRole.MECHANICAL_WOLF -> R.color.role_mech_wolf
        Player.MarkedRole.NONE -> R.color.gray_light
    }
    return resources.getColor(colorRes, null)
}

// Extension to get role display name
fun Player.MarkedRole.getDisplayName(): String {
    return when (this) {
        Player.MarkedRole.NONE -> ""
        Player.MarkedRole.SEER -> "预言家"
        Player.MarkedRole.SEER_MIRROR -> "通灵师"
        Player.MarkedRole.GOOD -> "好人"
        Player.MarkedRole.WEREWOLF -> "狼人"
        Player.MarkedRole.VILLAGER -> "平民"
        Player.MarkedRole.WITCH -> "女巫"
        Player.MarkedRole.HUNTER -> "猎人"
        Player.MarkedRole.GUARD -> "守卫"
        Player.MarkedRole.KNIGHT -> "骑士"
        Player.MarkedRole.IDIOT -> "白痴"
        Player.MarkedRole.WOLF_KING -> "狼王"
        Player.MarkedRole.WOLF_BEAUTY -> "狼美人"
        Player.MarkedRole.MECHANICAL_WOLF -> "机械狼"
    }
}
```

- [ ] **Step 2: 创建dialog_mark_role.xml布局**

```xml
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Good faction section -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="【好人阵营】"
        android:textColor="@color/border_good"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <LinearLayout
        android:id="@+id/goodRolesContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="【狼人阵营】"
        android:textColor="@color/wolf_red"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <LinearLayout
        android:id="@+id/evilRolesContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center" />

</LinearLayout>
```

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/voicewolf/app/MainActivity.kt app/src/main/res/layout/dialog_mark_role.xml
git commit -m "$(cat <<'EOF'
feat: 角色选择弹窗改为动态显示版型角色

- showMarkRoleDialog根据当前版型生成角色按钮
- 新增dialog_mark_role.xml分组布局
- 新增getRoleBackgroundColor方法

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: 修改updatePlayerView支持新角色颜色

**Files:**
- Modify: `app/src/main/java/com/voicewolf/app/MainActivity.kt:126-152`

- [ ] **Step 1: 扩展updatePlayerView中的角色颜色映射**

将`updatePlayerView`方法中的bgColorRes映射替换为：

```kotlin
val bgColorRes = when (player.markedRole) {
    Player.MarkedRole.SEER -> R.color.role_seer
    Player.MarkedRole.SEER_MIRROR -> R.color.role_seer_mirror
    Player.MarkedRole.WITCH -> R.color.role_witch
    Player.MarkedRole.HUNTER -> R.color.role_hunter
    Player.MarkedRole.GUARD -> R.color.role_guard
    Player.MarkedRole.KNIGHT -> R.color.role_knight
    Player.MarkedRole.IDIOT -> R.color.role_idiot
    Player.MarkedRole.GOOD -> R.color.role_good
    Player.MarkedRole.VILLAGER -> R.color.role_villager
    Player.MarkedRole.WEREWOLF -> R.color.role_werewolf
    Player.MarkedRole.WOLF_KING -> R.color.role_wolf_king
    Player.MarkedRole.WOLF_BEAUTY -> R.color.role_wolf_beauty
    Player.MarkedRole.MECHANICAL_WOLF -> R.color.role_mech_wolf
    Player.MarkedRole.NONE -> R.color.gray_light
}
```

- [ ] **Step 2: 验证编译通过**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: Commit**

```bash
git add app/src/main/java/com/voicewolf/app/MainActivity.kt
git commit -m "$(cat <<'EOF'
feat: updatePlayerView支持新角色颜色显示

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: 修改重置游戏弹窗为三选项

**Files:**
- Modify: `app/src/main/java/com/voicewolf/app/MainActivity.kt:440-450`

- [ ] **Step 1: 重写showResetConfirmDialog方法**

将`showResetConfirmDialog`方法替换为：

```kotlin
private fun showResetConfirmDialog() {
    val currentSetup = viewModel.currentSetup.value
    val setupName = currentSetup?.name ?: "未选择"
    
    val options = arrayOf(
        "重新选择版型",
        "继续使用当前版型（$setupName）",
        "取消"
    )
    
    AlertDialog.Builder(this)
        .setTitle("重置游戏")
        .setItems(options) { _, which ->
            when (which) {
                0 -> {
                    // 重新选择版型：清空记录并跳转到SetupActivity
                    viewModel.resetGame(keepSetup = false)
                    val intent = Intent(this, SetupActivity::class.java)
                    startActivity(intent)
                    finish()
                }
                1 -> {
                    // 继续使用当前版型：只清空记录
                    viewModel.resetGame(keepSetup = true)
                    Toast.makeText(this, "游戏已重置，保持版型：$setupName", Toast.LENGTH_SHORT).show()
                }
                2 -> {
                    // 取消：不做任何操作
                }
            }
        }
        .show()
}
```

- [ ] **Step 2: 添加Intent import**

确保文件顶部有：

```kotlin
import android.content.Intent
```

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/voicewolf/app/MainActivity.kt
git commit -m "$(cat <<'EOF'
feat: 重置弹窗改为三选项（重新选版型/继续用当前/取消）

- 选项0跳转SetupActivity
- 选项1保持版型清空记录
- 选项2取消

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 12: 验证完整功能并提交

- [ ] **Step 1: 构建完整APK**

Run: `./gradlew assembleDebug`
Expected: BUILD SUCCESSFUL，生成APK在`app/build/outputs/apk/debug/`

- [ ] **Step 2: 确认所有更改已提交**

Run: `git status`
Expected: working tree clean（无未提交更改）

- [ ] **Step 3: 查看提交历史确认完整**

Run: `git log --oneline -15`
Expected: 显示本次功能的多个commit

- [ ] **Step 4: 推送到远程（如需要）**

Run: `git push origin dev`
（可选，根据用户需求）

---

## Spec Coverage Check

| Spec Requirement | Task |
|-----------------|------|
| 新增GameSetup数据类 | Task 1 |
| 预置4种版型 | Task 1 |
| MarkedRole新增3角色 | Task 2 |
| 新增角色颜色 | Task 3 |
| GameViewModel新增currentSetup | Task 4 |
| 人数范围改为12-15 | Task 4 |
| SetupActivity布局 | Task 5 |
| SetupActivity页面 | Task 6 |
| SetupActivity设为启动 | Task 7 |
| MainActivity接收版型 | Task 8 |
| 角色弹窗动态显示 | Task 9 |
| updatePlayerView新角色 | Task 10 |
| 重置三选项 | Task 11 |

---

## Notes

- 本项目无单元测试，验证通过`./gradlew assembleDebug`构建成功
- 需在真机或模拟器上手动测试完整流程
- 发言模板按钮根据版型显示的功能暂未实现（当前dialog_add_speech.xml无模板按钮），如需实现需额外Task