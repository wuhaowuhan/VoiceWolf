<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/playerCard"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:cardCornerRadius="30dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/white"
    app:strokeWidth="2dp"
    app:strokeColor="@color/border_default">

    <FrameLayout
        android:layout_width="60dp"
        android:layout_height="60dp">

        <!-- Player avatar/background -->
        <ImageView
            android:id="@+id/playerAvatar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/player_avatar" />

        <!-- Player number overlay -->
        <TextView
            android:id="@+id/playerNumber"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/player_background"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Dead overlay -->
        <FrameLayout
            android:id="@+id/deadOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/dead_overlay"
            android:visibility="gone">

            <!-- Dead X mark -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="X"
                android:textColor="@color/white"
                android:textSize="24sp"
                android:textStyle="bold" />
        </FrameLayout>

        <!-- Pencil icon button for quick role marking -->
        <ImageButton
            android:id="@+id/btnMarkRole"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="top|end"
            android:layout_margin="2dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="标记身份"
            android:padding="2dp"
            android:scaleType="fitCenter"
            android:src="@android:drawable/ic_menu_edit"
            android:tint="@color/white"
            android:visibility="visible" />

        <!-- Remove button for players 13-15 -->
        <ImageButton
            android:id="@+id/btnRemove"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="2dp"
            android:background="@color/wolf_red"
            android:contentDescription="移除玩家"
            android:padding="2dp"
            android:scaleType="fitCenter"
            android:src="@android:drawable/ic_menu_delete"
            android:tint="@color/white"
            android:visibility="gone" />

        <!-- Role text badge (bottom) -->
        <TextView
            android:id="@+id/roleText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="#80000000"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="9sp"
            android:textStyle="bold"
            android:visibility="gone" />
    </FrameLayout>
</androidx.cardview.widget.CardView>